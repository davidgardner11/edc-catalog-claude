# dependencies
node_modules/
.pnpm-store/

# nuxt build output
.nuxt/
.output/
dist/
.data/

# ingest pipeline: downloaded originals, never committed
.ingest-cache/

# Product photos are sourced from brand/retailer CDNs and are copyrighted.
# This repo is public, so they are not committed (ADR-012).
# A fresh clone must run `pnpm ingest` to populate them.
public/images/

# env & local config
.env
.env.*
!.env.example

# editor / os
.DS_Store
*.log
.vscode/
.idea/

# test artifacts
test-results/
playwright-report/
coverage/
